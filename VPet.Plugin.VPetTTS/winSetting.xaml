<Window x:Class="VPet.Plugin.VPetTTS.winSetting"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ll="clr-namespace:LinePutScript.Localization.WPF;assembly=LinePutScript.Localization.WPF"
        mc:Ignorable="d"
        Title="{ll:Str VPetTTS 设置}" Height="650" Width="520"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        Background="#F5F5F5"
        Closed="Window_Closed">
    
    <Window.Resources>
        <!-- 主色调 -->
        <SolidColorBrush x:Key="PrimaryColor" Color="#5B9BD5"/>
        <SolidColorBrush x:Key="PrimaryHoverColor" Color="#4A8BC4"/>
        <SolidColorBrush x:Key="AccentColor" Color="#70AD47"/>
        <SolidColorBrush x:Key="DangerColor" Color="#E74C3C"/>
        <SolidColorBrush x:Key="CardBackground" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="BorderColor" Color="#E0E0E0"/>
        <SolidColorBrush x:Key="TextColor" Color="#333333"/>
        <SolidColorBrush x:Key="SubTextColor" Color="#666666"/>
        
        <!-- 卡片样式 -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBackground}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.1" Color="#000000"/>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 标题样式 -->
        <Style x:Key="CardTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>
        
        <!-- 按钮样式 -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryHoverColor}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#CCCCCC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E8F4FD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="AccentButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#5F9A3C"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#CCCCCC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 开关样式 -->
        <Style x:Key="ToggleSwitchStyle" TargetType="CheckBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal">
                            <Grid Width="44" Height="24" Cursor="Hand">
                                <Border x:Name="track" Background="#CCCCCC" CornerRadius="12"/>
                                <Border x:Name="thumb" Width="20" Height="20" Background="White" 
                                        CornerRadius="10" HorizontalAlignment="Left" Margin="2,0,0,0">
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="4" ShadowDepth="1" Opacity="0.3"/>
                                    </Border.Effect>
                                </Border>
                            </Grid>
                            <ContentPresenter Margin="10,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="track" Property="Background" Value="{StaticResource PrimaryColor}"/>
                                <Setter TargetName="thumb" Property="HorizontalAlignment" Value="Right"/>
                                <Setter TargetName="thumb" Property="Margin" Value="0,0,2,0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 滑块样式 -->
        <Style x:Key="ModernSliderStyle" TargetType="Slider">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid>
                            <Border Height="6" Background="#E0E0E0" CornerRadius="3" VerticalAlignment="Center"/>
                            <Track x:Name="PART_Track">
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Ellipse Width="18" Height="18" Fill="{StaticResource PrimaryColor}" Cursor="Hand">
                                                    <Ellipse.Effect>
                                                        <DropShadowEffect BlurRadius="6" ShadowDepth="2" Opacity="0.3"/>
                                                    </Ellipse.Effect>
                                                </Ellipse>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 下拉框样式 - 使用简单样式避免显示问题 -->
        <Style x:Key="ModernComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="MinHeight" Value="36"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
                </Trigger>
                <Trigger Property="IsDropDownOpen" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- 文本框样式 -->
        <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题区域 -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="🔊 VPetTTS" FontSize="24" FontWeight="Bold" 
                       Foreground="{StaticResource TextColor}" HorizontalAlignment="Center"/>
            <TextBlock Text="{ll:Str 为你的桌宠添加语音}" FontSize="12" 
                       Foreground="{StaticResource SubTextColor}" HorizontalAlignment="Center" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- 设置内容 -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="0,0,8,0">
            <StackPanel>
                <!-- 基本设置卡片 -->
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="{ll:Str ⚙️ 基本设置}" Style="{StaticResource CardTitleStyle}"/>
                        
                        <CheckBox x:Name="SwitchOn" Style="{StaticResource ToggleSwitchStyle}" 
                                  Content="{ll:Str 启用 VPetTTS}" Margin="0,0,0,16"/>
                        
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="50"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{ll:Str 音量}" VerticalAlignment="Center" Foreground="{StaticResource SubTextColor}"/>
                            <Slider x:Name="VolumeSilder" Grid.Column="1" Minimum="0" Maximum="200" Value="100" 
                                    Style="{StaticResource ModernSliderStyle}" VerticalAlignment="Center" Margin="10,0"/>
                            <TextBlock x:Name="VolumeText" Grid.Column="2" Text="100%" 
                                       VerticalAlignment="Center" HorizontalAlignment="Right"
                                       Foreground="{StaticResource PrimaryColor}" FontWeight="SemiBold"/>
                        </Grid>
                        
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="50"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{ll:Str 语速}" VerticalAlignment="Center" Foreground="{StaticResource SubTextColor}"/>
                            <Slider x:Name="SpeedSilder" Grid.Column="1" Minimum="0.1" Maximum="3.0" Value="1.0" 
                                    Style="{StaticResource ModernSliderStyle}" VerticalAlignment="Center" Margin="10,0"/>
                            <TextBlock x:Name="SpeedText" Grid.Column="2" Text="1.0x" 
                                       VerticalAlignment="Center" HorizontalAlignment="Right"
                                       Foreground="{StaticResource PrimaryColor}" FontWeight="SemiBold"/>
                        </Grid>
                        
                        <CheckBox x:Name="EnableCache" Style="{StaticResource ToggleSwitchStyle}" 
                                  Content="{ll:Str 启用缓存（提高响应速度）}" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <!-- TTS提供商卡片 -->
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="{ll:Str 🎤 TTS 提供商}" Style="{StaticResource CardTitleStyle}"/>
                        <ComboBox x:Name="CombProvider" Padding="10,8" FontSize="13" MinHeight="36">
                            <ComboBoxItem Content="{ll:Str 🆓 Free TTS（免费）}" Tag="Free"/>
                            <ComboBoxItem Content="{ll:Str 🤖 OpenAI TTS}" Tag="OpenAI"/>
                            <ComboBoxItem Content="{ll:Str 🎵 GPT-SoVITS}" Tag="GPT-SoVITS"/>
                            <ComboBoxItem Content="{ll:Str 🔗 自定义 URL}" Tag="URL"/>
                            <ComboBoxItem Content="{ll:Str 🛠️ DIY TTS}" Tag="DIY"/>
                        </ComboBox>
                    </StackPanel>
                </Border>

                <!-- 提供商配置卡片 -->
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="{ll:Str 📝 提供商配置}" Style="{StaticResource CardTitleStyle}"/>
                        <StackPanel x:Name="ProviderConfigPanel">
                            <!-- 动态内容将在这里显示 -->
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- 代理设置卡片 -->
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="{ll:Str 🌐 代理设置}" Style="{StaticResource CardTitleStyle}"/>
                        
                        <CheckBox x:Name="EnableProxy" Style="{StaticResource ToggleSwitchStyle}" 
                                  Content="{ll:Str 启用代理}" Margin="0,0,0,12"/>
                        <CheckBox x:Name="FollowSystemProxy" Style="{StaticResource ToggleSwitchStyle}" 
                                  Content="{ll:Str 跟随系统代理}" Margin="0,0,0,12"/>
                        
                        <TextBlock Text="{ll:Str 代理地址}" Foreground="{StaticResource SubTextColor}" Margin="0,0,0,6"/>
                        <TextBox x:Name="ProxyAddress" Style="{StaticResource ModernTextBoxStyle}" 
                                 Text="127.0.0.1:8080" Margin="0,0,0,12"/>
                        
                        <TextBlock Text="{ll:Str 协议}" Foreground="{StaticResource SubTextColor}" Margin="0,0,0,6"/>
                        <ComboBox x:Name="ProxyProtocol" Padding="10,8" FontSize="13" MinHeight="36">
                            <ComboBoxItem Content="HTTP" Tag="http"/>
                            <ComboBoxItem Content="HTTPS" Tag="https"/>
                            <ComboBoxItem Content="SOCKS5" Tag="socks5"/>
                        </ComboBox>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- 按钮区域 -->
        <Border Grid.Row="2" Background="{StaticResource CardBackground}" CornerRadius="8" 
                Padding="16" Margin="0,12,0,0">
            <Border.Effect>
                <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.1" Direction="90"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button x:Name="Test" Content="{ll:Str 🔊 测试}" Grid.Column="0" 
                        Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,6,0" Click="Test_Click"/>
                <Button x:Name="ClearCache" Content="{ll:Str 🗑️ 清理}" Grid.Column="1" 
                        Style="{StaticResource SecondaryButtonStyle}" Margin="6,0,6,0" Click="ClearCache_Click"/>
                <Button x:Name="Save" Content="{ll:Str ✅ 保存}" Grid.Column="2" 
                        Style="{StaticResource PrimaryButtonStyle}" Margin="6,0,6,0" Click="Save_Click"/>
                <Button x:Name="Cancel" Content="{ll:Str ❌ 关闭}" Grid.Column="3" 
                        Style="{StaticResource SecondaryButtonStyle}" Margin="6,0,0,0" Click="Cancel_Click"/>
            </Grid>
        </Border>
    </Grid>
</Window>